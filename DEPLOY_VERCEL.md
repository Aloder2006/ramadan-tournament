# 🚀 نشر المشروع على Vercel (Deployment Guide)

إليك طريقة رفع المشروع على منصة **Vercel** المجانية.

## 1. التجهيز (Setup)

لقد قمت بإعداد الملفات اللازمة (`vercel.json` وتعديل `server.js` و `db.js`).
كل ما عليك فعله هو رفع التعديلات إلى GitHub:

```bash
git add .
git commit -m "Configure for Vercel deployment"
git push
```

## 2. إنشاء مشروع على Vercel

1. اذهب إلى [Vercel.com](https://vercel.com) وسجّل حساباً.
2. اضغط **Add New > Project**.
3. اختر مستودع **ramadan-tournament** من قائمة GitHub واضغط **Import**.

## 3. الإعدادات (Configure Project)

في صفحة الإعداد "Configure Project":

1. **Framework Preset:** اختر `Vite` (أو سيتم اكتشافه تلقائياً).
2. **Root Directory:** (اتركه `client` إذا طلب منك، لكن بما أننا نستخدم `vercel.json` في الجذر، فالأفضل تركه فارغاً `Root: ./`).
   - **تنبيه:** إذا اكتشف Vercel أن المشروع هو "Vite" وحاول تغيير المسار، تأكد أنك في الجذر (root settings).
   - *ملاحظة:* ملف `vercel.json` الذي أضفته يتولى توجيه المسارات الصحيحة، لذا الإعدادات الافتراضية للجذر عادة تكفي.
   
3. **Environment Variables:** أضف المتغيرات التالية (ضروري جداً):
   - `MONGO_URI`: رابط قاعدة بيانات MongoDB Atlas.
   - `JWT_SECRET`: أي نص عشوائي.

4. **Output Directory:**
   - غير المجلد الافتراضي إلى: `client/dist`
   - (لأن ملفات React المبنية ستكون داخل هذا المجلد)

5. اضغط **Deploy**.

## 4. ما بعد النشر

- **قاعدة البيانات:** تأكد من أنك سمحت بالوصول من أي مكان (`0.0.0.0/0`) في إعدادات Network Access في MongoDB Atlas، لأن Vercel يستخدم عناوين IP متغيرة.
- **تحديث البيانات:** إذا قمت بتعديل الكود ورفعه (`git push`)، سيقوم Vercel بإعادة النشر تلقائياً.

---

### ❓ مشاكل شائعة

- **خطأ 404 في الـ API:**
  تأكد أن `vercel.json` موجود في الجذر.
  تأكد أن المسار في الفرونت إند هو `/api/...` (وهذا مضبوط بالفعل).

- **خطأ اتصال قاعدة البيانات:**
  تأكد من `MONGO_URI` وصلاحيات الشبكة (IP Whitelist) في Atlas.
