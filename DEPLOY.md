# 🚀 دليل رفع وتشغيل المشروع (Deployment Guide)

هذا الدليل يشرح كيفية رفع مشروع "دوري رمضان" وتشغيله على الإنترنت مجاناً باستخدام خدمة **Render**.

## 1. التجهيز (Local Setup)

تأكد من أنك قمت بتثبيت Git وإنشاء مستودع (Repository).
لقد قمت بالفعل بإنشاء ملفات التكوين اللازمة (`package.json`, `.gitignore`).

قم بتنفيذ الأوامر التالية في مجلد المشروع لحفظ التغييرات ورفعها إلى GitHub:

```bash
git init
git add .
git commit -m "Initial commit - Ramadan Tournament App"
# (قم بإنشاء مستودع جديد على GitHub ثم اربطه)
git remote add origin https://github.com/YOUR_USERNAME/ramadan-tournament.git
git push -u origin master
```

## 2. النشر على Render (Deployment)

1. اذهب إلى [Render.com](https://render.com) وسجّل حساباً.
2. اضغط على **New +** واختر **Web Service**.
3. اربط حساب GitHub واختر مستودع المشروع الذي رفعته للتو.
4. املأ البيانات كالتالي:
   - **Name:** `ramadan-tournament` (أو أي اسم)
   - **Region:** اختر الأقرب لك (مثلاً Frankfurt)
   - **Branch:** `master` (أو `main`)
   - **Root Directory:** (اتركه فارغاً)
   - **Runtime:** `Node`
   - **Build Command:** `npm run build`
   - **Start Command:** `npm start`
5. انزل للأسفل إلى قسم **Environment Variables** وأضف المتغيرات التالية (انسخها من ملف `.env` لديك):
   - `MONGO_URI`: رابط قاعدة البيانات (يجب أن تكون قاعدة بيانات سحابية مثل MongoDB Atlas)
   - `JWT_SECRET`: أي نص سري عشوائي
   - `PORT`: `5000` (Render سيقوم بتعيينه تلقائياً ولكن وضعه احتياطاً جيد)

6. اضغط **Create Web Service**.

## 3. قاعدة البيانات (MongoDB Atlas)

إذا كنت تستخدم قاعدة بيانات محلية (`mongodb://localhost...`)، فلن تعمل على السيرفر. يجب عليك إنشاء قاعدة بيانات سحابية مجانية على [MongoDB Atlas](https://www.mongodb.com/cloud/atlas).
بعد الإنشاء، احصل على رابط الاتصال (Connection String) واستخدمه في المتغير `MONGO_URI` في الخطوة السابقة.

---

## 🛠️ ملاحظات
- المشروع مُعدّ ليعمل كوحدة واحدة (Monorepo).
- الأمر `npm run build` سيقوم بتثبيت مكتبات السيرفر والعميل، ثم بناء واجهة React ووضعها في مجلد `client/dist`.
- السيرفر (`server.js`) مبرمج ليقوم بخدمة ملفات الواجهة (`client/dist`) تلقائياً.
